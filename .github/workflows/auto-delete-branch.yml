---
name: Auto Delete Merged Branch

# 在 PR 關閉時觸發
on:
  pull_request:
    types:
      - closed

jobs:
  delete-branch:
    # 僅在 PR 被合併（非單純關閉）時執行
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Delete merged branch
        run: |
          echo "PR #${{ github.event.pull_request.number }} 已被合併"
          echo "來源分支: ${{ github.event.pull_request.head.ref }}"
          echo "目標分支: ${{ github.event.pull_request.base.ref }}"
          
          # 刪除遠端分支
          git push origin --delete ${{ github.event.pull_request.head.ref }} || echo "分支已被刪除或不存在"
          
      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🎉 分支 `${{ github.event.pull_request.head.ref }}` 已自動刪除'
            })
