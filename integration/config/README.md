# Home Assistant Development Config

This directory is mounted as `/config` inside the Home Assistant container.

Home Assistant will create runtime files here after the first boot. Those files
usually contain local state or secrets and should stay out of git.
